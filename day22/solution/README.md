# Solution Workspace - Day 22

Add your own C source files here (`*.c`, `*.h`).

## Local Commands

From this directory:

```bash
make test
make asan
make valgrind
make clean
```

From repository root:

```bash
make day=22 test
make day=22 asan
make day=22 valgrind
make day=22 clean
```

## Expected Minimum

- At least one C source file (for example `main.c`)
- Code compatible with C11 and strict warning flags
